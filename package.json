{
  "name": "better-auth-offline-monorepo",
  "private": true,
  "workspaces": ["better-auth-offline", "example"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "packageManager": "bun@1.3.10",
  "devDependencies": {
    "turbo": "^2"
  }
}
